# Conformance fixtures

Normative test vectors for `mono-sync/0.2.0-draft`. See [spec/CONFORMANCE.md](../spec/CONFORMANCE.md).

Regenerate from the reference implementation:

```bash
cd ../rust && cargo run -p gen-golden
```
